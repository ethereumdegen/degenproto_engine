[package]
name = "degenproto_engine"
version = "0.2.1"
edition = "2024"
description = "Rust library for parsing RON configuration files and generating React/JSX code"
license = "MIT"

[dependencies]
ron = "0.8"
serde = { version = "1.0", features = ["derive"] }
